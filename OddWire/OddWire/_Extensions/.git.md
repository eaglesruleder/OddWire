# Brief — Extensions

*[OddWire](../../../README.md) › Extensions*

**Scope:** Subsystem
**Domain:** OddWire
**Status:** Active

---

## What it is
Shared engine-glue helpers used across the mod — extension methods over the Vintage Story API and over base System types. No gameplay of its own; it exists so features don't re-implement the same plumbing.

---

## Areas

| Area | What it is | Brief | Doc |
|---|---|---|---|
| VintageStory API | env sampling, stack resolution, shape/transform/tesselation, and TreeAttribute (de)serialization for engine property types | [.git.md](VintageStory/.git.md) | [.gpt.md](VintageStory/.gpt.md) |
| System | tiny base-type helpers — `string.EndWith(char)`, `float`/`double`.`Approx(epsilon)` | — | — |
